# Copy to credentials.toml and fill in real values.
# credentials.toml is gitignored — never commit it.

[sources.example]
url = "https://webdav.example.org/share"
username = "user"
password = "secret"
root = "content-dir"  # directory on the share that holds the book directories
